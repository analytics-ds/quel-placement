{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">
{{- $siteLang := .Site.Language.Lang }}
{{- $langPrefix := printf "/%s/" $siteLang }}
{{- range .Data.Pages -}}
{{- $isSiteLangPage := false }}
{{- if eq $siteLang "fr" }}
  {{- if not (hasPrefix .RelPermalink "/en/") }}{{ $isSiteLangPage = true }}{{ end }}
{{- else }}
  {{- if hasPrefix .RelPermalink $langPrefix }}{{ $isSiteLangPage = true }}{{ end }}
{{- end }}
{{- if and $isSiteLangPage (not .Params.private) (not .Params.noindex) }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ safeHTML ( .Lastmod.Format "2006-01-02T15:04:05-07:00" ) }}</lastmod>
    {{- end }}
    {{- with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>
    {{- end }}
    {{- if ge .Sitemap.Priority 0.0 }}
    <priority>{{ .Sitemap.Priority }}</priority>
    {{- end }}
    {{- if .IsTranslated }}
    {{- $seen := slice $siteLang }}
    <xhtml:link rel="alternate" hreflang="{{ $siteLang }}" href="{{ .Permalink }}" />
    {{- range .Translations }}
    {{- $tLang := .Lang }}
    {{- if hasPrefix .RelPermalink "/en/" }}{{ $tLang = "en" }}{{ else }}{{ $tLang = "fr" }}{{ end }}
    {{- if not (in $seen $tLang) }}
    <xhtml:link rel="alternate" hreflang="{{ $tLang }}" href="{{ .Permalink }}" />
    {{- $seen = $seen | append $tLang }}
    {{- end }}
    {{- end }}
    {{- end }}
  </url>
{{- end }}
{{- end }}
</urlset>
